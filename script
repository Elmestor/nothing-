local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))() local Window = Rayfield:CreateWindow({ Name = "MULTIHACK v2.2", LoadingTitle = "jarmini 2.0", LoadingSubtitle = "by Berry", ConfigurationSaving = { Enabled = false }, Discord = { Enabled = false }, KeySystem = false }) local Players = game:GetService("Players") local RunService = game:GetService("RunService") local Workspace = game:GetService("Workspace") local debris = game:GetService("Debris") local LocalPlayer = Players.LocalPlayer local PlayerGui = LocalPlayer:WaitForChild("PlayerGui") local liftEnabled, deadPullActive, autoCrownEnabled, randomHitboxActive, targetRandomPlayer = false, false, false, false, nil local monsterEspActive, runnersEspActive = false, false local whitelistedPlayers, ringModel, ringRadius, segments, segmentThickness, segmentHeight, segmentParts, updateConn, highlightMap, isActive, lastRadius = {}, nil, 0, 72, 1.0, 0.06, {}, nil, {}, false, nil local avoidOffsetExtra, teleportCooldown, perPlayerCooldown, _lastTeleportTime, playerLastTeleported = 3, 0.001, 0.001, 0, {} local teleportSoundIDs = {"rbxassetid://87566211283329", "rbxassetid://81857580097150", "rbxassetid://129561737395908", "rbxassetid://122312400582724", "rbxassetid://136080815136211"} local soundPlayOrder, currentSoundIndex, soundsEnabled = {1,2,3,1,2,4,5,4}, 0, true local espData = {} task.spawn(function() local iFolder = LocalPlayer:FindFirstChild("IFolder") or LocalPlayer:WaitForChild("IFolder", 5) if iFolder then for _, item in ipairs(iFolder:GetDescendants()) do if item:IsA("BoolValue") then item.Value = true end end end end) local function getMonsterRoot() for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then local name = p.Name:lower() local teamName = p.Team and p.Team.Name:lower() or "" if name:match("monst") or teamName:match("monst") then return p.Character.HumanoidRootPart end end end return nil end local function isRagdolled(targetPlayer) local char = targetPlayer.Character if not char then return false end local humanoid = char:FindFirstChildOfClass("Humanoid") if humanoid and (humanoid.Health <= 0 or humanoid.PlatformStand == true) then return true end for _, child in ipairs(char:GetDescendants()) do if child.Name:lower():match("ragdoll") or child:IsA("BallSocketConstraint") then return true end end return false end local function getRandomValidPlayer() local validPlayers = {} for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then local teamName = p.Team and p.Team.Name:lower() or "" if not teamName:match("monst") and not p.Name:lower():match("monst") then table.insert(validPlayers, p) end end end return #validPlayers > 0 and validPlayers[math.random(1, #validPlayers)] or nil end local function playTeleportSound() if not soundsEnabled then return end currentSoundIndex = (currentSoundIndex % #soundPlayOrder) + 1 local soundId = teleportSoundIDs[soundPlayOrder[currentSoundIndex]] if not soundId then return end local sound = Instance.new("Sound", Workspace) sound.SoundId = soundId sound.Volume = 0.7 sound.Ended:Connect(function() pcall(function() sound:Destroy() end) end) pcall(function() sound:Play() end) end local function isPositionClear(fromPos, toPos, ignoreList) local dir = toPos - fromPos local dist = dir.Magnitude if dist <= 0.01 then return true end local params = RaycastParams.new() params.FilterDescendantsInstances = ignoreList or {} params.FilterType = Enum.RaycastFilterType.Blacklist if Workspace:Raycast(fromPos, dir.Unit * dist, params) then return false end local downResult = Workspace:Raycast(toPos + Vector3.new(0, 5, 0), Vector3.new(0, -50, 0), params) return (downResult and downResult.Instance and downResult.Instance.CanCollide) and true or false end local function findSafeTeleport(myPos, intrPos, baseOffset, ignoreList) local dir = myPos - intrPos dir = Vector3.new(dir.X, 0, dir.Z) dir = dir.Magnitude < 0.001 and Vector3.new(1,0,0) or dir.Unit local left = Vector3.new(-dir.Z, 0, dir.X) local right = Vector3.new(dir.Z, 0, -dir.X) local candidates = {myPos + left * baseOffset, myPos + right * baseOffset} for i = 1, 8 do local ang = (math.pi/16) * i table.insert(candidates, myPos + (CFrame.fromOrientation(0, ang, 0) * CFrame.new(left)).Position.Unit * baseOffset) table.insert(candidates, myPos + (CFrame.fromOrientation(0, -ang, 0) * CFrame.new(right)).Position.Unit * baseOffset) end for _, cand in ipairs(candidates) do if isPositionClear(myPos + Vector3.new(0, 2, 0), Vector3.new(cand.X, myPos.Y, cand.Z) + Vector3.new(0, 2, 0), ignoreList) then return Vector3.new(cand.X, myPos.Y, cand.Z) + Vector3.new(0, 2, 0) end end return nil end local function clearRing() if updateConn then updateConn:Disconnect(); updateConn = nil end if ringModel then ringModel:Destroy(); ringModel = nil end segmentParts = {} for pl, hl in pairs(highlightMap) do if hl then hl:Destroy() end end highlightMap = {} isActive = false end local function createRing(radius) clearRing() if not radius or radius <= 0 then return end ringRadius = radius local char = LocalPlayer.Character if not char or not char.PrimaryPart then return end ringModel = Instance.new("Model", Workspace) ringModel.Name = "UltraInstinctRing" for i = 1, segments do local angle = (i-1) * (2 * math.pi / segments) local arcLen = (2 * math.pi * radius) / segments local seg = Instance.new("Part") seg.Anchored = true seg.CanCollide = false seg.CastShadow = false seg.Size = Vector3.new(segmentThickness, segmentHeight, math.max(0.05, arcLen + 0.02)) seg.Material = Enum.Material.Neon seg.Color = Color3.fromRGB(220, 30, 30) seg.Parent = ringModel table.insert(segmentParts, seg) end updateConn = RunService.RenderStepped:Connect(function() local myChar = LocalPlayer.Character if not myChar or not myChar.PrimaryPart then clearRing(); return end local centerPos = myChar.PrimaryPart.Position for i, seg in ipairs(segmentParts) do local angle = (i-1) * (2 * math.pi / #segmentParts) seg.CFrame = CFrame.new(centerPos + Vector3.new(math.cos(angle) * ringRadius, 0.12, math.sin(angle) * ringRadius)) * CFrame.Angles(0, -angle, 0) end for _, pl in pairs(Players:GetPlayers()) do if pl ~= LocalPlayer then if whitelistedPlayers[pl.Name] == true then local ch = pl.Character if ch and ch.PrimaryPart and ch:FindFirstChildWhichIsA("Humanoid") then local dist = (ch.PrimaryPart.Position - centerPos).Magnitude if dist <= ringRadius + 0.001 then if not highlightMap[pl] then local hl = Instance.new("Highlight", PlayerGui) hl.Adornee = ch hl.FillColor = Color3.fromRGB(255, 30, 30) hl.FillTransparency = 0.5 highlightMap[pl] = hl end local now = tick() if now - _lastTeleportTime >= teleportCooldown and (not playerLastTeleported[pl] or now - playerLastTeleported[pl] >= perPlayerCooldown) then local safe = findSafeTeleport(centerPos, ch.PrimaryPart.Position, ringRadius + avoidOffsetExtra, {myChar, ringModel}) if safe then pcall(function() myChar.PrimaryPart.CFrame = CFrame.new(safe) end) playTeleportSound() _lastTeleportTime = now playerLastTeleported[pl] = now end end else if highlightMap[pl] then highlightMap[pl]:Destroy(); highlightMap[pl] = nil end end else if highlightMap[pl] then highlightMap[pl]:Destroy(); highlightMap[pl] = nil end end else if highlightMap[pl] then highlightMap[pl]:Destroy(); highlightMap[pl] = nil end end else if highlightMap[pl] then highlightMap[pl]:Destroy(); highlightMap[pl] = nil end end end end) end local function clearEspData(p) if espData[p] then pcall(function() if espData[p].Highlight then espData[p].Highlight:Destroy() end if espData[p].Attachment0 then espData[p].Attachment0:Destroy() end if espData[p].Attachment1 then espData[p].Attachment1:Destroy() end if espData[p].Beam then espData[p].Beam:Destroy() end if espData[p].Billboard then espData[p].Billboard:Destroy() end end) espData[p] = nil end end local TabMain = Window:CreateTab("MAIN", nil) local TabUI = Window:CreateTab("ULTRA INSTINCT", nil) local TabESP = Window:CreateTab("ESP", nil) local TabWhitelist = Window:CreateTab("WHITELIST", nil) TabMain:CreateToggle({ Name = "LIFT (Авто-Лифт)", CurrentValue = false, Callback = function(Value) liftEnabled = Value end, }) TabMain:CreateButton({ Name = "TRAP (Поставить капкан)", Callback = function() local char = LocalPlayer.Character local classScripts = char and char:FindFirstChild("ClassScripts") local target = classScripts and classScripts:FindFirstChild("TrapperClassServer") local event = target and target:FindFirstChild("TrapperPlaceEvent") if event then event:FireServer() end end, }) TabMain:CreateButton({ Name = "RND HITBOX (Притянуть рандома на 1с)", Callback = function() if not randomHitboxActive then local chosenPlayer = getRandomValidPlayer() if chosenPlayer then targetRandomPlayer = chosenPlayer randomHitboxActive = true task.delay(1, function() randomHitboxActive = false targetRandomPlayer = nil end) end end end, }) TabMain:CreateButton({ Name = "DEAD PULL (Стянуть лежачих на 1с)", Callback = function() if not deadPullActive then deadPullActive = true task.delay(1, function() deadPullActive = false end) end end, }) TabMain:CreateToggle({ Name = "AUTOCROWN (Авто-Корона)", CurrentValue = false, Callback = function(Value) autoCrownEnabled = Value end, }) TabMain:CreateButton({ Name = "Открыть Mobile Dex", Callback = function() pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Keyless-Mobile-Dex-54729"))() end) end, }) TabMain:CreateButton({ Name = "Avatar Changer", Callback = function() pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/darkdexv2/universalavatarchanger/main/avatarchanger"))() end) end, }) TabMain:CreateButton({ Name = "INFINITE YIELD", Callback = function() pcall(function() loadstring(game:HttpGet('https://raw.githubusercontent.com/DarkNetworks/Infinite-Yield/main/latest.lua'))() end) end, }) local RangeInput = TabUI:CreateInput({ Name = "Радиус кольца", PlaceholderText = "Введите радиус (например, 50)", RemoveTextAfterFocusLost = false, Callback = function(Text) end, }) TabUI:CreateToggle({ Name = "ULTRA INSTINCT (Анти-Сближение)", CurrentValue = false, Callback = function(Value) if Value then local num = tonumber(RangeInput.Value) if not num or num <= 0 then Rayfield:Notify({Name = "Ошибка", Content = "Некорректный радиус!", Duration = 2}) return end lastRadius = math.clamp(num, 1, 2000) createRing(lastRadius) isActive = true else clearRing() isActive = false lastRadius = nil end end, }) TabUI:CreateToggle({ Name = "Звуки телепортации", CurrentValue = true, Callback = function(Value) soundsEnabled = Value end, }) TabESP:CreateToggle({ Name = "MONSTER ESP", CurrentValue = false, Callback = function(Value) monsterEspActive = Value end, }) TabESP:CreateToggle({ Name = "RUNNERS ESP", CurrentValue = false, Callback = function(Value) runnersEspActive = Value end, }) local WhitelistDropdown = TabWhitelist:CreateDropdown({ Name = "Выберите игроков для атаки (Кольца)", Options = {}, CurrentOption = {}, MultipleOptions = true, Callback = function(Options) for name in pairs(whitelistedPlayers) do whitelistedPlayers[name] = false end for _, name in ipairs(Options) do whitelistedPlayers[name] = true end end, }) local function updateDropdownPlayers() local names = {} for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then table.insert(names, p.Name) if whitelistedPlayers[p.Name] == nil then whitelistedPlayers[p.Name] = false end end end WhitelistDropdown:Refresh(names, WhitelistDropdown.CurrentOption) end Players.PlayerAdded:Connect(updateDropdownPlayers) Players.PlayerRemoving:Connect(function(p) whitelistedPlayers[p.Name] = nil if highlightMap[p] then highlightMap[p]:Destroy(); highlightMap[p] = nil end playerLastTeleported[p] = nil clearEspData(p) updateDropdownPlayers() end) updateDropdownPlayers() task.spawn(function() local processedTraps = {} local crownTouched = false local lastCheckTime = 0 while true do pcall(function() local myChar = LocalPlayer.Character local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart") if not myHrp then return end local nowTime = tick() if autoCrownEnabled then local parkourFolder = Workspace:FindFirstChild("Parkour") if parkourFolder then local crown = parkourFolder:FindFirstChild("CrownDisplay") if crown and crown:IsA("BasePart") and crown:FindFirstChild("TouchInterest") then if not crownTouched then if firetouchinterest then firetouchinterest(myHrp, crown, 0) task.wait(0.01) firetouchinterest(myHrp, crown, 1) else local oldCFrame = myHrp.CFrame myHrp.CFrame = crown.CFrame task.wait(0.05) myHrp.CFrame = oldCFrame end crownTouched = true end else crownTouched = false end end end if liftEnabled then local endRoom = Workspace:FindFirstChild("Rooms") and Workspace.Rooms:FindFirstChild("EndRoom") if endRoom then local elevators = {endRoom:FindFirstChild("Elevator1"), endRoom:FindFirstChild("Elevator2")} for _, elevator in ipairs(elevators) do if elevator then local exitSign = elevator:FindFirstChild("ExitSign") if exitSign then myHrp.CFrame = exitSign:GetPivot() * CFrame.new(0, 3, 0) end local hitPart = elevator:FindFirstChild("HitPart") if hitPart then myHrp.CFrame = hitPart:GetPivot() myHrp.Velocity = Vector3.new(0, 0, 0) end end end end end if randomHitboxActive and targetRandomPlayer and targetRandomPlayer.Character then local enemyPart = targetRandomPlayer.Character:FindFirstChild("HumanoidRootPart") or targetRandomPlayer.Character:FindFirstChild("Torso") if enemyPart then enemyPart.CFrame = myHrp.CFrame if enemyPart:IsA("BasePart") then enemyPart.Velocity = Vector3.new(0, 0, 0) end end end if deadPullActive then for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer and p.Character and isRagdolled(p) then local enemyPart = p.Character:FindFirstChild("HumanoidRootPart") or p.Character:FindFirstChild("Torso") if enemyPart then enemyPart.CFrame = myHrp.CFrame if enemyPart:IsA("BasePart") then enemyPart.Velocity = Vector3.new(0, 0, 0) end end end end end if nowTime - lastCheckTime >= 0.05 then lastCheckTime = nowTime local monsterRoot = getMonsterRoot() if monsterRoot then for _, obj in ipairs(Workspace:GetChildren()) do if obj.Name == "Caltrop" and not processedTraps[obj] then processedTraps[obj] = true if obj:IsA("BasePart") then obj.CanCollide = false; obj.Size = Vector3.new(8, 8, 8); obj.CFrame = monsterRoot.CFrame; debris:AddItem(obj, 1) elseif obj:IsA("Model") then for _, child in ipairs(obj:GetDescendants()) do if child:IsA("BasePart") then child.CanCollide = false; child.Size = Vector3.new(8, 8, 8) end end obj:PivotTo(monsterRoot.CFrame); debris:AddItem(obj, 1) end end end end for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then local char = p.Character local hrp = char and char:FindFirstChild("HumanoidRootPart") local hum = char and char:FindFirstChildWhichIsA("Humanoid") if hrp and hum and hum.Health > 0 then local nameLower = p.Name:lower() local teamLower = p.Team and p.Team.Name:lower() or "" local isMonst = nameLower:match("monst") or teamLower:match("monst") local isRunner = teamLower:match("runner") or (not isMonst) local espEnabled = (isMonst and monsterEspActive) or (isRunner and runnersEspActive) if espEnabled then local color = isMonst and Color3.fromRGB(255, 30, 30) or Color3.fromRGB(30, 220, 220) local dist = math.floor((myHrp.Position - hrp.Position).Magnitude) if not espData[p] then local data = {} data.Highlight = Instance.new("Highlight") data.Highlight.FillTransparency = 0.6; data.Highlight.OutlineTransparency = 0.1; data.Highlight.Parent = char data.Attachment0 = Instance.new("Attachment", myHrp) data.Attachment1 = Instance.new("Attachment", hrp) data.Beam = Instance.new("Beam") data.Beam.Width0 = 0.1; data.Beam.Width1 = 0.1; data.Beam.FaceCamera = true data.Beam.Attachment0 = data.Attachment0; data.Beam.Attachment1 = data.Attachment1; data.Beam.Parent = myHrp data.Billboard = Instance.new("BillboardGui") data.Billboard.Size = UDim2.new(0, 100, 0, 30); data.Billboard.AlwaysOnTop = true; data.Billboard.ExtentsOffset = Vector3.new(0, 3, 0) local lbl = Instance.new("TextLabel", data.Billboard) lbl.Size = UDim2.new(1, 0, 1, 0); lbl.BackgroundTransparency = 1 lbl.Font = Enum.Font.SourceSansBold; lbl.TextSize = 13; lbl.TextStrokeTransparency = 0 data.TextLabel = lbl; data.Billboard.Parent = hrp espData[p] = data end local d = espData[p] d.Highlight.Adornee = char; d.Highlight.FillColor = color; d.Highlight.OutlineColor = color d.Beam.Color = ColorSequence.new(color); d.TextLabel.TextColor3 = color; d.TextLabel.Text = p.DisplayName .. " [" .. dist .. "m]" if d.Attachment1.Parent ~= hrp then d.Attachment1.Parent = hrp end else clearEspData(p) end else clearEspData(p) end end end end end) task.wait(0.01) end end) LocalPlayer.CharacterAdded:Connect(function() task.wait(0.2) if lastRadius and lastRadius > 0 and isActive then createRing(lastRadius) end end)
